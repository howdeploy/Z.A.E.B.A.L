# Z.A.E.B.A.L. PROTOCOL — LEVEL 1 (user is swearing)

**False-trigger check.** Decide whether the profanity is addressed to you. If this is a meta-mention of Z.A.E.B.A.L. or is about the outside world, say in one line that the trigger is false and why, then keep working. Silently ignoring the protocol is forbidden.

1. **STOP.** The user believes you did something obviously stupid or repeated your own mistake. Do not perform the next action until you complete the protocol.
2. **Independent checks.** Launch two independent sub-agent auditors with RAW artifacts: the user's latest messages verbatim, diffs or changed files, and test/error logs. Sub-agents are always **internal** auditors; only a verdict inside `<zaebal-verdict>` is external. If session policy or the environment prevents sub-agent launch, state that in one line and perform the belief inventory yourself. Silently skipping this step or relabeling internal agents as external is forbidden.
3. **Belief inventory.** List what you consider facts about the task and mark each item `FACT` only when confirmed by a run, file, or log; otherwise mark it `HYPOTHESIS`. Check every hypothesis or remove it from the reasoning. Mandatory checks when relevant:
   - "written ≠ took effect" — prove that every config, hook, instruction, or env variable is consumed from the path where you placed it;
   - "healthy ≠ serving the intended traffic" — if observed service behavior contradicts tests, status, or logs, enumerate all candidate instances on the workstation and every in-scope server, including similarly named containers/services; compare their image/version, command, environment, mounts, network, ports, routing, credentials, and ownership, then prove which exact instance consumes a real request;
   - "command variation ≠ diagnosis" — after reproducing a command failure once, check the installed version/help and current official documentation or internet before changing flags, subcommand order, or spelling. Do not try cosmetic syntax variants from memory.
4. **Micro-plan.** Choose a concrete correction: roll back/fix, move state into a file and shrink the session, hand off into a clean chat, or continue with a corrected TODO.
5. **Notify the human.** State the wrong belief, the audit result, and the plan.

**Contract check.** If the user says this protocol requires X, verify that claim against this document before agreeing: answer either "the contract requires Y; your expectation differs" or "yes, I violated item N."

**Completion gate before the final response:** (1) the wrong belief is corrected; (2) every literal constraint of the original request is satisfied; (3) there is a verification artifact. If any item fails, keep working or report the precise blocker.
